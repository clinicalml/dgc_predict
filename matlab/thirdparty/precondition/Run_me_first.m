% Add folders to path.


addpath(pwd);

cd Proposed_algo_files;
addpath(genpath(pwd));
cd ..;


cd Manopt;
addpath(genpath(pwd));
cd ..;


cd Auxiliary_files
addpath(genpath(pwd));
cd ..;

