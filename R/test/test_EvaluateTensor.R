TestComputeGCP = function(){
  warning('add test')
  #GCP = ComputeGCP(T_meas, T_pred_list, plot=FALSE, subset='cv', file=NA)
}

TestComputeCSpPres = function(){
  #Csp = ComputeCSpPres(T_meas, T_pred_list, plot=FALSE, subset='cv', cs_true=NULL, debug=FALSE)
  warning('add test')
}

TestComputePCT_AllModes = function(){
  warning('add test')
  #ComputePCT_AllModes(T_meas, T_pred_list)
}

TestComputePCT = function(){
  warning('add test')
  #PCT = ComputePCT(T_meas, T_pred)
}

TestComputePCTPerSig = function(){
  warning('add test')
  #PCTs = ComputePCTPerSig(T1, T2, format='df')
}

TestTensor2Vec = function(){
  warning('add test')
  #vec = Tensor2Vec(T1, T2)
}
